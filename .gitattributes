# Set default behaviour for text files
* text=auto

# Defines the type of files to normalize end of line.
*.sh text
*.bat text

# Defines end of line for these kind of files in the source control.
# Scripts
*.bash     text eol=lf
*.fish     text eol=lf
*.sh       text eol=lf
# These are explicitly windows files and should use crlf
*.bat      text eol=crlf
*.cmd      text eol=crlf

# Exclude files from exporting
.gitattributes export-ignore
.gitignore     export-ignore
.gitkeep       export-ignore
